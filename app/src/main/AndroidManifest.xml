<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Add INTERNET permission if your app needs to fetch anything from internet for notifications -->
    <!-- <uses-permission android:name="android.permission.INTERNET" /> -->

    <!-- Required for scheduling exact alarms on Android 12 (API 31) and higher -->
    <!-- If your targetSdkVersion is 31 or higher, you might need this for exact alarms. -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />

    <!-- Required for posting notifications on Android 13 (API 33) and higher -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>

    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

<!--    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />-->
<!--    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="28" />-->
<!--    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" android:maxSdkVersion="28"  />-->

<!--    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />-->
<!--    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />-->
    <uses-permission android:name="android.permission.READ_MEDIA_VISUAL_USER_SELECTED" />

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".TimetableApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CortexaApp"
        tools:targetApi="31"
        >

        <!-- NEW: Declare TimetableDetailsActivity -->
        <activity android:name=".timetable.TimetableDetailsActivity" />

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="com.google.mlkit.vision.DEPENDENCIES"
            android:value="ocr,ocr_chinese,ocr_devanagari,ocr_japanese,ocr_korea" />
        <!--            android:value="ocr" >-->

        <meta-data
            android:name="com.google.firebase.ml.vision.DEPENDENCIES"
            android:value="ocr,model2,model3" />

<!--        <activity android:name=".TestMapPickerActivity"-->
<!--            android:exported="true">-->
<!--            <intent-filter>-->
<!--                <action android:name="android.intent.action.MAIN" />-->
<!--                <category android:name="android.intent.category.LAUNCHER" />-->
<!--            </intent-filter>-->
<!--        </activity>-->

<!--        <activity-->
<!--            android:name=".test.TestLatLongDiscoveryActivity"-->
<!--            android:exported="true">-->
<!--            <intent-filter>-->
<!--                <action android:name="android.intent.action.MAIN" />-->
<!--                <category android:name="android.intent.category.LAUNCHER" />-->
<!--            </intent-filter>-->
<!--        </activity>-->

        <!-- Declare the NotificationReceiver -->
        <receiver
            android:name=".NotificationReceiver"
            android:enabled="true"
            android:exported="false"> <!-- Set to false as it's an internal receiver -->
            <intent-filter>
                <action android:name="com.example.timetableapp.NOTIFICATION_ACTION" />
            </intent-filter>
        </receiver>

        <!-- NEW: Declare TimetableDetailsActivity -->
        <activity android:name=".preferences.PreferencesActivity" />

        <activity
            android:name=".timetable.AddTimetableActivity"
            android:theme="@style/Theme.Material3.DayNight.Dialog.Alert" />

        <!-- NEW: Declare AddEngagementActivity -->
        <activity android:name=".timetable.ViewTimetablesActivity" />

        <!-- NEW: Declare AddEngagementActivity -->
        <activity android:name=".engagement.AddEngagementActivity" />

        <!-- NEW: Declare AddEngagementActivity -->
        <activity android:name=".engagement.EngagementListActivity" />

        <!-- NEW: Declare AddAssignmentActivity -->
        <activity android:name=".assignment.AddAssignmentActivity" />

        <!-- NEW: Declare AssignmentListActivity -->
        <activity android:name=".assignment.AssignmentListActivity" />

        <!-- NEW: Declare AddExamActivity -->
        <activity android:name=".exam.AddExamActivity" />

        <!-- NEW: Declare ExamListActivity -->
        <activity android:name=".exam.ExamListActivity" />

        <!-- NEW: Declare AddTodoActivity -->
        <activity android:name=".todo.AddTodoActivity" />

        <!-- NEW: Declare TodoListActivity -->
        <activity android:name=".todo.TodoListActivity" />

        <!-- NEW: Declare AddNoteActivity -->
        <activity android:name=".note.AddNoteActivity" />

        <!-- NEW: Declare NotesListActivity -->
        <activity android:name=".note.NotesListActivity" />

        <!-- NEW: Declare AddNoteActivity -->
        <activity android:name=".action.AddActionActivity" />

        <!-- NEW: Declare NotesListActivity -->
        <activity android:name=".action.ActionListActivity" />

        <!-- NEW: Declare RecordAudioActivity -->
        <activity android:name=".util.audio.RecordAudioActivity" />

        <!-- NEW: Declare ScheduleActivity -->
        <activity android:name=".ScheduleActivity" />

        <!-- NEW: Declare ImageToPdfActivity -->
<!--        <activity android:name=".util.image2pdf.ImageToPdfActivity" />-->

        <!-- Declare ImageEditorActivity -->
<!--        <activity android:name=".util.image2pdf.ImageEditorActivity" />-->
<!--        <activity android:name=".util.image2pdf.mlkit.old.MLKitScannerActivity" />-->
        <activity android:name=".util.image2pdf.mlkit.DocumentScannerActivity" />

        <!-- Declare MapPickerActivity -->
        <activity android:name=".util.location.MapPickerActivity" />

        <!-- Declare MapPickerActivity -->
        <activity android:name=".util.location.MapPickerPaidActivity" />

        <activity android:name=".external.calendar.ViewCalendarEventActivity" />

        <!-- NEW: Declare ClashDetectionActivity; NOT NEEDED NOW -->
        <!-- <activity android:name=".ClashDetectionActivity" />-->

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- WorkManager for background tasks (like CleanupWorker) -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup" />
        </provider>

    </application>

</manifest>
